[package]
name = "pyxirr"
version = "0.2.0"
authors = ["Anexen"]
edition = "2018"
description = "Rust-powered collection of financial functions."
readme = "README.md"
homepage = "https://github.com/Anexen/pyxirr"
keywords = ["financial", "xirr", "xnpv"]

[package.metadata.maturin]
classifiers = [
    "Topic :: Office/Business :: Financial",
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
]
provides-extra = ["bench"]
requires-dist = [
    "xirr==0.1.6; extra == 'bench'",
    "finance-calculator==0.0.6; extra == 'bench'",
]

[lib]
name = "pyxirr"
crate-type = ["rlib", "cdylib"]

[[bench]]
name = "main"
harness = false

[dependencies]
pyo3 = { version = "0.13" }
chrono = "0.4"
financial = "1.1.2"

[dev-dependencies]
criterion = {version = "0.3", features = ["html_reports"] }

[features]
default = ["pyo3/extension-module"]
tests = ["pyo3/auto-initialize"]
